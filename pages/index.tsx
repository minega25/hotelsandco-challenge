import Layout from "@/components/Layout";
import RoomSlider from "@/components/RoomSlider";

const Home = () => {
  return (
    <Layout>
      <RoomSlider />
    </Layout>
  );
};

export default Home;
